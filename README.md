# MikroTrack Bootstrap

Це стартовий каркас проєкту MikroTrack для локального запуску через Docker Compose.

## Швидкий старт

1. Створіть `.env` файл на основі прикладу:

```bash
cp .env.example .env
```

2. Запустіть застосунок:

```bash
docker compose up --build
```
